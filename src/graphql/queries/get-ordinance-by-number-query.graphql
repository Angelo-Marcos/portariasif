query GetOrdinanceByNumber($number: String!) {
  ordinance(where: {number: $number}, stage: DRAFT) {
    id
    number
    ordinanceType
    effectiveStartDate
    effectiveEndDate
    members {
      name
    }
  }
}
