query GetOrdinancesByMemberName($name: String!) {
  members(where: {name_starts_with: $name}, stage: DRAFT) {
    name
    ordinances {
      ... on Ordinance {
        number
        ordinanceType
        effectiveStartDate
        effectiveEndDate
      }
    }
  }
}