query GetOrdinancesByMemberName($name: String!) {
  members(where: {name_starts_with: $name}, stage: DRAFT) {
    name
    memberType
    workload
    ordinances {
      ... on Ordinance {
        number
        ordinanceType
        effectiveStartDate
        effectiveEndDate
      }
    }
  }
}