query GetOrdinances {
    ordinances(orderBy: effectiveEndDate_ASC, stage: DRAFT) {
        id
        number
        effectiveEndDate
        effectiveStartDate
        members {
            name
        }
        ordinanceType
        subject
    }
}