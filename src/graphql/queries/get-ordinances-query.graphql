query GetOrdinances {
    ordinances(orderBy: effectiveEndDate_ASC, stage: DRAFT) {
        id
        number
        effectiveEndDate
        members {
            name
        }
        ordinanceType
        subject
    }
}