query GetOrdinances {
    ordinances(orderBy: effectiveStartDate_DESC, stage: DRAFT) {
        id
        number
        effectiveStartDate
        members {
            name
        }
        ordinanceType
        subject
    }
}